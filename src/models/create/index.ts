// import typeofJsonc from "typeof-jsonc";
// import { translate } from "./servers";

import { getParams } from "../../utils/params"


// const preParams = (option: string) => {
//   try {
//     if (option && )
//   } catch (error) {
    
//   }
// }
const run = () => {
  console.log(getParams())
}

export default run